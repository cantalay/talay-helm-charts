{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["image", "containerPort", "runtimeConfig"],
  "properties": {
    "replicaCount": { "type": "integer", "minimum": 1 },
    "image": {
      "type": "object",
      "required": ["repository", "tag"],
      "properties": {
        "repository": { "type": "string", "minLength": 1 },
        "tag": { "type": "string", "minLength": 1 },
        "digest": { "type": "string" },
        "pullPolicy": { "enum": ["Always", "IfNotPresent", "Never"] }
      }
    },
    "containerPort": { "type": "integer", "minimum": 1, "maximum": 65535 },
    "runtimeConfig": { "type": "object" },
    "securityHeaders": {
      "type": "object",
      "properties": {
        "contentSecurityPolicy": { "type": "string" },
        "strictTransportSecurity": { "type": "string", "minLength": 1 },
        "permissionsPolicy": { "type": "string", "minLength": 1 },
        "referrerPolicy": { "type": "string", "minLength": 1 },
        "xFrameOptions": { "enum": ["DENY", "SAMEORIGIN"] }
      }
    },
    "networkPolicy": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "denyEgress": { "type": "boolean" }
      }
    },
    "registrySecret": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "remoteKey": { "type": "string" },
        "targetName": { "type": "string", "minLength": 1 }
      },
      "allOf": [
        {
          "if": { "properties": { "enabled": { "const": true } }, "required": ["enabled"] },
          "then": { "properties": { "remoteKey": { "minLength": 1 } }, "required": ["remoteKey"] }
        }
      ]
    }
  }
}
